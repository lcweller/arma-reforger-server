# Optional overrides (defaults are already correct for the provided compose file)
SERVER_DIR=/app/data/server
CONFIG_DIR=/app/data/config
APP_ID=1874900
AUTO_UPDATE=true
VALIDATE_ON_UPDATE=false
CHOWN_ON_START=false